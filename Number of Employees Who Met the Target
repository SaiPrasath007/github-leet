int numberOfEmployeesWhoMetTarget(int* hours, int hoursize, int target)
{
    int count=0;
    for(int i=0;i<hoursize;i++)
    {
        if(hours[i]>=target)
        {
            count+=1;
        }
    }
    return count;
}
