int addDigits(int num) 
{
    int sum1,sum2,dig;
    sum2=num;
    while(sum2>9)
    {
        sum1=sum2;
        sum2=0;
        while(sum1>0)
        {
            dig=sum1%10;
            sum2+=dig;
            sum1/=10;
        }
    }
    return sum2;        
}
