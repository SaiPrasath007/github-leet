bool isPalindrome(int x)
{
    int dig,n;
    long long int r=0;
    n=x;
    while(x!=0)
    {
        dig=x%10;
        r=(r*10)+dig;
        x=x/10;
    }
    if(n<0)
        return false;
    else if(n!=r)
        return false;
    else
        return true;
}
